package com.example.commoncode.viewpager

/*
import androidx.fragment.app.Fragment
import androidx.fragment.app.FragmentManager
import androidx.lifecycle.Lifecycle
import androidx.viewpager2.adapter.FragmentStateAdapter
import whistlephonefinder.findphonebywhistle.ClapToFindFragment
import whistlephonefinder.findphonebywhistle.WhistleToFindFragment

private const val NUM_TABS = 2

public class HowToUseViewPagerAdapter(fragmentManager: FragmentManager, lifecycle: Lifecycle) :
    FragmentStateAdapter(fragmentManager, lifecycle) {

    override fun getItemCount(): Int {
        return NUM_TABS
    }

    override fun createFragment(position: Int): Fragment {
        when (position) {
            0 -> return ClapToFindFragment()
            1 -> return WhistleToFindFragment()
        }
        return ClapToFindFragment()
    }
}*/
